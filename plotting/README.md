Download files locally from Wandb in `download-data.ipynb`.

Run cells in order in `analyze-data.ipynb` to reproduce plots.

Each plot was made using data from one sweep, and consists of two cells: a cell defining a function and a cell that calls that function.

Do not use `main.ipynb`.
